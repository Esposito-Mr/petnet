# .github/workflows/windows_release.yml

name: Build & Release Windows App (Portable)

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows
        run: flutter build windows --release

      # --- NEW DEBUGGING STEP ---
      # This will show us what folders were created by the build command
      - name: List build directory contents
        run: |
          echo "--- Listing contents of build/windows/runner ---"
          Get-ChildItem -Path build/windows/runner
        shell: pwsh
      # --- END OF DEBUGGING STEP ---

      - name: Zip portable build
        run: Compress-Archive -Path build/windows/runner/Release\* -DestinationPath "windows-portable-${{ github.ref_name }}.zip"
        shell: pwsh

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          prerelease: false
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref }}
          files: |
            windows-portable-${{ github.ref_name }}.zip